# 项目核心依赖
discord.py[speed]
python-dotenv
tzdata  # Windows 下 zoneinfo 的时区数据源（IANA 库），必须安装
Pillow
requests
aiohttp
google-genai
google-api-core
PyYAML
uvloop; sys_platform != 'win32'
# PostgreSQL and ORM dependencies
SQLAlchemy
asyncpg
pgvector
alembic

# Data Processing & AI
langchain-text-splitters
numpy==1.26.4

# Web 管理后台
fastapi
uvicorn[standard]

httpx
python-multipart  # FastAPI 文件上传（multipart/form-data）必需

# 上传文档解析与切块（md/pdf/docx/xlsx → 统一 Markdown）
pymupdf4llm
python-docx
openpyxl

# 测试依赖
pytest
pytest-asyncio
pytest-dotenv
# This is for Alembic's sync operations, our main app still uses asyncpg
psycopg2-binary
